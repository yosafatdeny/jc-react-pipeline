pipeline {
  agent any

  environment{
    CI = true
  }

  stages {
    
    stage ('Install Dependencies react project'){
      steps{
        echo "Starts install dependencies react project"
        sh "npm install"
      }
    }
      
  }      
}